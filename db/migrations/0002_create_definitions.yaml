schema: 3
name: CreateDefinitions
changes:
  up:
    - |
      CREATE TABLE definitions (
          id INTEGER NOT NULL PRIMARY KEY,
          word_id int4 NOT NULL REFERENCES words(id) UNIQUE,
          definitions TEXT NOT NULL
      )
  down:
    - DROP TABLE definitions
